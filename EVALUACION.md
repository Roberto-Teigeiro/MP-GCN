# Evaluación del Proyecto MP-GCN

| Proyecto | Rubros | Indicadores | Argumento |
|----------|--------|-------------|-----------|
| MP-GCN | Metodología | Hace un levantamiento de requerimientos completo y correcto | Se realizaron varias sesiones de preguntas y respuestas con el socio formador para saber los requisitos del entregable y la calidad esperada de este mismo |
| | | Aplica metodología de manera correcta | train.py y trainer.py ejercen un ciclo ML robusto: separación trainval (create_data_loaders), reproducibilidad (np.random_seed en dataset.py), WarmupCosineScheduler y save_checkpoint.py. |
| | | Mantiene una comunicación clara y efectiva con el socio formador | Cada semana o 2 se tenía una sesión propia con el equipo y el socio formador para aclarar las dudas respecto al desarrollo |
| | Documentación | Genera documentación clara y completa del proceso | En el repositorio de github se muestra el paper de investigación, así como la estructura del código en este mismo readme, explicando un poco que es cada parte de |
| | | El repositorio del proyecto está bien estructurado y cumple con estándares | Estructura clara y bien organizada: /model (código fuente), /deliverables (resultados), /checkpoints (modelos), inference.py (script principal), README.md (documentación), .git/ (control de versiones). Usa convenciones estándar: módulos organizados, configuración centralizada en config.py, notebooks de prueba en test_model.py. Archivo training_full.log para registros. |
| | Datos | Identifica si es necesario el uso de big data para la solución del reto y explica correctamente su razonamiento | Big Data: SÍ es necesario. El proyecto explica su razonamiento: (1) Dataset: múltiples videos de playground (pose_batch.zip), requiere procesamiento de flujos de video, (2) Dimensionalidad: cada persona genera 17 keypoints × 48 frames × 6 streams (J,B,JM,BM) = alto volumen, (3) Escalabilidad: maneja 6 personas simultáneamente por frame × múltiples videos, (4) Procesamiento: usa DataLoader con batch processing, normalización, y almacenamiento en .npy para eficiencia, (5) Motivo técnico: requiere GPU (CUDA detectado en código), mixed precision (AMP), y procesamiento paralelo para manejar volumen. README explica la necesidad de pose extraction de videos (YOLO batching) |
| | Repositorio de Github y distribución por equipo | Disfruta un modelo de almacenamiento y manejo de datos adhoc al reto | No se implementó/no fue requerido en el reto |
| | Modelo | Selecciona un modelo adecuado al problema presentado en el reto | Se utilizó MP-GCN (Multi-Person Graph Convolutional Network) cual es adecuado para reconocimiento de actividades de esqueleto entre varias personas en el plano. Usa arquitecturas SOTA: ST-GCN (Spatio-Temporal Graph Convolutional Networks), procesamiento multi-stream (Joint, Bone, JointMotion, BoneMotion), bloques de atención, normalización por batch, y técnicas avanzadas como Mixed Precision (AMP). Estructura modular con SpatialGraphConv, TemporalConv, y bloques STGCNBlock. |
| | | Emplea arquitecturas del estado del arte deep learning como parte de la solución del reto | Usa arquitecturas SOTA: ST-GCN (Spatio-Temporal Graph Convolutional Networks), procesamiento multi-stream (Joint, Bone, Motion), bloques de atención, normalización por batch, y técnicas avanzadas como Mixed Precision (AMP). Estructura modular con SpatialGraphConv, TemporalConv, y bloques STGCNBlock. Define dos variantes: PlaygroundGCN (completa con 4 streams) y PlaygroundGCNLite (ligera con 2 streams). |
| | | Elabora un modelo benchmark (regresión lineal, logística o un modelo anima) para la evaluación de la solución y explica detalladamente la forma en la que se genera el modelo | No implementado en la solución de reto |
| | | Plantea pruebas del estado del arte respaladas en la literatura, que sean válidas y relevantes en el contexto del reto | Se buscaron trabajos similares en cuanto a esta área de investigación y se tomó como diferencia el paper "Skeleton based Group Activity Recognition using Spatio-Temporal Panoramic Graph". Implementa métricas estándar en action recognition: Accuracy, F1 weighted, F1 macro, matriz de confusión, precision/recall por clase. |
| | Evaluación | Selecciona métricas (o indicadores de desempeño) adecuadas tanto para el modelo como para el reto | Métricas bien seleccionadas: Accuracy (82.54%), F1 weighted (78.19%), F1 macro (42.88%), matriz de confusión, precision/recall por clase. Almacenadas en evaluation.json y training_summary.json. Relevantes para clasificación multiclase de actividades. Genera visualizaciones: confusion_matrix.jpg y training_history.jpg. |
| | | Interpreta los resultados del modelo de manera correcta | Genera reportes de clasificación por clase con precision, recall, F1-score. Crea matriz de confusión visualizada (confusion_matrix.jpg). El código en visualize_and_evaluate.py interpreta explícitamente qué clases funcionan bien y cuáles tienen issues. Analiza por clase: Social_People (82.93%), Transit (79.55%), Play_Object_Normal (0%), Adult_Assisting (37.5%), sliding (0%). |
| | Refinamiento | Refina el modelo después de su primera iteración para mejorar su desempeño | Implementa refinamientos en trainer.py: label smoothing (0.1), mixup augmentation (α=0.2), dropout (0.3), grad clipping (max_norm=1.0), warmup learning rate schedule con cosine decay. Checkpoints saved: best_model.pth y final_model.pth. Early stopping logic implementada con monitoreo de best_acc y best_f1. |
| | | Prueba con otras arquitecturas (además del benchmark) para ver si obtiene mejores rendimientos | Define dos variantes: PlaygroundGCN (completa) y PlaygroundGCNLite (ligera), y train_model(..., lite=True) selecciona entre ellas. Para comparar: modelo más simple y uno más robusto con arquitecturas similares. Permite experimentación con diferentes configuraciones (num_stages, base_channels, dropout). |

